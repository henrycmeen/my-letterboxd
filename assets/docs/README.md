# Documentation Media Assets

This folder stores image assets used by documentation pages/README files.

- Keep only reusable doc images here.
- Keep runtime visuals in `public/`.
- Keep temporary debug screenshots out of git (or under ignored `docs/references/*` folders).
