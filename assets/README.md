# Assets

Source assets that are not served directly at runtime.

## Subfolders

- `vhs-mockups/`
  Purchased/local design source files (PSD/PSB workflows).
- `docs/`
  Images used by project documentation (README/docs pages).

## Rule

If a file must be served in the app, put it in `public/` instead.
